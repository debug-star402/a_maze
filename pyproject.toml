[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "mazegen"
version = "1.0.0"
description = "A perfect and imperfect maze generator for 42 Network."
readme = "README.md"
requires-python = ">=3.10"

[tool.flake8]
max-line-length = 99
